import { Sequelize } from 'sequelize';

export default (function () {

    const env = process.env;

    // return new Sequelize(db, user, password, optionsObject: {host: xxxx, port: xxxx, dialect: 'postgres'})



})();